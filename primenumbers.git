def is_prime(num):
        for j in range(2,num):
                if(num%j) == 0:
                        return False
        return True


usNum = input('Enter a usable number:')
numArray = []

for i in range(2,usNum):
        if is_prime(i):
                numArray.append(i)
print numArray


